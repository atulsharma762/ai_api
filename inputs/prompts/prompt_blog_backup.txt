This topic: #topic_name# will be used to generate a high-quality, long-form blog post for a technical learning platform.

Your task is to return an exhaustive, well-structured, beautifully formatted JSON array in the format below:

[
  {
    "sub_topic_name": "string",
    "content": [
      {
        "format": "text" | "text_code" | "text_quote" | "code" | "image",
        "detail": [
          {
            "type": "text" | "code" | "quote" | "image",
            "content": "For type = text: Use readable, semantic HTML with clear inline CSS. All text must use <p>, <ul>, <ol>, <table>, etc., and be styled with larger, accessible font (e.g., font-size: 18px, line-height: 1.7). Break long explanations into multiple 'content' entries to maintain row-by-row display.

Quotes must be added using format: type: 'quote', but ONLY where helpful — do not force a quote. Use quotes for principles, insights, or mental models, not for filler.

Tables should be clean, with borders, cell padding, and spacing via inline CSS. Hyperlinks must use target='_blank' and be styled properly. Code blocks must be syntactically correct and plain text (no highlighting)."
          }
        ]
      }
    ]
  }
]

Guidelines:

1. You will auto-infer all relevant subtopics — no sub-topic list will be provided.
2. Return **strictly valid JSON only** — no Markdown, no explanation text outside the array.
3. Do NOT repeat `sub_topic_name` inside HTML. No `<h3>What is X?</h3>` duplication.
4. Use `"text_code"` when pairing explanation and code. It must have one `"text"` and one `"code"` detail entry.
5. For `"text"`, break into multiple content objects if there are multiple paragraphs — one logical idea per block.
6. For `"text_quote"`, include **insightful or motivational quotes**, but only where contextually relevant. The quote must be plain text (no HTML).
7. For `"image"`, provide either an external image URL or describe the image (e.g. "a diagram showing how promises work").
8. All HTML inside `"type": "text"` must be styled with:
   - `font-size: 18px;`
   - `line-height: 1.7;`
   - `margin-bottom: 12px;`
   - Semantic tags like `<p>`, `<ul>`, `<strong>`, `<table>` used properly
9. Tables must include proper `<thead>` and `<tbody>` structure with inline CSS for borders and spacing.
10. Include real examples, edge cases, best practices, alternatives, and tooling insights from beginner to advanced.
11. You must ensure it renders vertically and clearly — avoid cramming multiple points into one HTML block.

Reminder: You will receive only `#topic_name#` as input. You must infer and structure everything else.
